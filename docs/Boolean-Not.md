# not

`(not VALUE)`

The function `not` returns `true` if VALUE is not
[truthy](Boolean-Truthy.md), and `false otherwise.

Examples:

```lisp
> (not true)
false

> (not false)
true

> (not (list))
true
```
