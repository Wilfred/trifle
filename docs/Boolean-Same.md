# same?

The function `same?` returns `true` if both arguments are of the same
type and represent the same value.

Examples:

    > (same? 1 1)
    true

    > (same? 1 2)
    false

    > (same? false 123)
    false
